# csd



To run the app use the following commands:

cd step2
docker-compose up --build
You can change the port number according to the service you want to use:
port: 8001 for events_management
port: 8002 for tickets_management